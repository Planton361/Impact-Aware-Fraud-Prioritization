{
  "name": "Impact-Aware Fraud Prioritization",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.12-bookworm",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "charliermarsh.ruff"
      ],
      "settings": {
        "python.defaultInterpreterPath": "${containerWorkspaceFolder}/.venv/bin/python"
      }
    }
  },
  "postCreateCommand": "test -x .venv/bin/python || python -m venv .venv && .venv/bin/python -m pip install pip==26.2 && .venv/bin/python -m pip install -r environment/final_experiment_requirements.txt && .venv/bin/python -m pip install -r environment/bootstrap_requirements.txt && .venv/bin/python -m pip install pytest==9.1.1 && .venv/bin/python -m pip install ruff==0.16.1 && .venv/bin/python -m pip install -e . --no-deps && .venv/bin/python -m pip check && .venv/bin/fraud-detection check --full"
}
